# План внедрения TeachHelper

Данный документ описывает пошаговый план развертывания системы TeachHelper в продакшн окружение.

## 📊 Статус выполнения

**Этап 3: Исправление критических проблем** ✅ **ЗАВЕРШЕН**

- ✅ CORS настроен через переменные окружения
- ✅ Исправлено использование get_boto_client() в service_files.py и service_comments.py
- ✅ Исправлен SQLAlchemy event listener в model_files.py
- ✅ Создана утилита для валидации файлов
- ✅ Все критические проблемы безопасности исправлены

**Следующий этап:** Этап 4 - Развертывание базы данных

## 📋 Содержание

1. [Предварительная подготовка](#предварительная-подготовка)
2. [Этап 1: Подготовка инфраструктуры](#этап-1-подготовка-инфраструктуры)
3. [Этап 2: Настройка окружения](#этап-2-настройка-окружения)
4. [Этап 3: Исправление критических проблем](#этап-3-исправление-критических-проблем)
5. [Этап 4: Развертывание базы данных](#этап-4-развертывание-базы-данных)
6. [Этап 5: Развертывание хранилища файлов](#этап-5-развертывание-хранилища-файлов)
7. [Этап 6: Развертывание приложения](#этап-6-развертывание-приложения)
8. [Этап 7: Настройка безопасности](#этап-7-настройка-безопасности)
9. [Этап 8: Мониторинг и логирование](#этап-8-мониторинг-и-логирование)
10. [Этап 9: Тестирование в продакшене](#этап-9-тестирование-в-продакшене)
11. [Этап 10: Запуск и поддержка](#этап-10-запуск-и-поддержка)

---

## Предварительная подготовка

### Необходимые ресурсы

- [ ] Сервер(ы) для развертывания (VPS, облако)
- [ ] Доменное имя (опционально, но рекомендуется)
- [ ] SSL сертификат (Let's Encrypt или коммерческий)
- [ ] Доступ к SMTP серверу для отправки email
- [ ] Резервное копирование настроено

### Команда

- [ ] DevOps инженер
- [ ] Backend разработчик
- [ ] Тестировщик (QA)

### Время на внедрение

**Минимальное время**: 2-3 рабочих дня  
**Рекомендуемое время**: 1 неделя (с учетом тестирования)

---

## Этап 1: Подготовка инфраструктуры

### 1.1 Выбор платформы развертывания

**Вариант A: Docker Compose (рекомендуется для начала)**
- ✅ Простота развертывания
- ✅ Легкое масштабирование
- ✅ Изоляция сервисов

**Вариант B: Kubernetes (для масштабируемости)**
- ✅ Высокая доступность
- ✅ Автомасштабирование
- ⚠️ Требует больше знаний

**Вариант C: Традиционный VPS**
- ✅ Полный контроль
- ⚠️ Требует ручной настройки

### 1.2 Требования к серверу

**Минимальные требования:**
- CPU: 2 ядра
- RAM: 4 GB
- Диск: 50 GB SSD
- Сеть: 100 Mbps

**Рекомендуемые требования:**
- CPU: 4 ядра
- RAM: 8 GB
- Диск: 100 GB SSD
- Сеть: 1 Gbps

### 1.3 Установка базового ПО

```bash
# Обновление системы (Ubuntu/Debian)
sudo apt update && sudo apt upgrade -y

# Установка Docker и Docker Compose
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh
sudo usermod -aG docker $USER

# Установка Docker Compose
sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
sudo chmod +x /usr/local/bin/docker-compose

# Установка Git
sudo apt install git -y
```

**Чеклист:**
- [ ] Docker установлен и работает
- [ ] Docker Compose установлен
- [ ] Пользователь добавлен в группу docker
- [ ] Git установлен

---

## Этап 2: Настройка окружения

### 2.1 Клонирование/загрузка проекта

```bash
# Если проект в Git
git clone <repository-url>
cd teachHelper

# Или загрузить архив проекта
```

### 2.2 Создание файла окружения

Создайте файл `.env` в корне проекта:

```bash
cp .env.example .env  # Если есть пример
# Или создайте вручную
nano .env
```

**Минимальная конфигурация для продакшена:**

```env
# Database
DATABASE_HOST=postgres_db
DATABASE_PORT=5432
DATABASE_USER=teachhelper_user
DATABASE_PASSWORD=<strong-password-here>
DATABASE_NAME=teachhelper_prod

# Security - ВАЖНО: Используйте сильные случайные ключи!
SECRET=<generate-strong-secret-32-chars-min>
SECRET_CONFIRM_KEY=<generate-strong-secret-32-chars-min>
SECRET_RESET_KEY=<generate-strong-secret-32-chars-min>
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=120

# Frontend URL
FRONT_URL=https://yourdomain.com

# MinIO
MINIO_HOST=minio
MINIO_PORT=9000
MINIO_USER=minioadmin
MINIO_PASSWORD=<strong-password-here>

# Email Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_TLS=true
SMTP_FROM_EMAIL=noreply@yourdomain.com
SMTP_FROM_NAME=TeachHelper

# Environment
ENVIRONMENT=production
DEBUG=false
```

### 2.3 Генерация секретных ключей

```bash
# Генерация случайных секретов
python -c "import secrets; print(secrets.token_urlsafe(32))"
# Выполните 3 раза для SECRET, SECRET_CONFIRM_KEY, SECRET_RESET_KEY
```

**Чеклист:**
- [ ] Файл `.env` создан
- [ ] Все секретные ключи сгенерированы и сохранены
- [ ] Пароли БД и MinIO установлены
- [ ] Email настройки проверены

---

## Этап 3: Исправление критических проблем

⚠️ **ВАЖНО**: Перед развертыванием необходимо исправить критические проблемы безопасности.

### 3.1 Исправление CORS ✅ ВЫПОЛНЕНО

**Файл**: `main.py`

```python
# БЫЛО:
allow_origins=["*"]

# ИСПРАВЛЕНО:
# CORS теперь настраивается через переменную окружения CORS_ORIGINS
# В .env укажите: CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
# Или оставьте "*" для разработки
```

**Изменения:**
- Добавлена переменная `CORS_ORIGINS` в `app/config/config_app.py`
- CORS настраивается динамически из переменных окружения
- Поддерживается список доменов через запятую

### 3.2 Исправление использования get_boto_client() ✅ ВЫПОЛНЕНО

**Файл**: `app/services/service_files.py`

```python
# БЫЛО:
s3 = get_boto_client()

# ИСПРАВЛЕНО:
async with get_boto_client() as s3:
    # использование s3
```

**Изменения:**
- Исправлено в `service_files.py` (методы `create` и `delete`)
- Исправлено в `service_comments.py` (метод `delete`)
- Исправлена проблема с `file.s3_key` - теперь используется `f"{file.id}/{file.filename}"`

### 3.3 Исправление SQLAlchemy event listener ✅ ВЫПОЛНЕНО

**Файл**: `app/models/model_files.py`

```python
# БЫЛО:
@event.listens_for(Files, 'before_delete')
async def delete_file_from_minio(...):

# ИСПРАВЛЕНО:
@event.listens_for(Files, 'before_delete')
def delete_file_from_minio(mapper, connection, target):
    # Синхронная функция с asyncio.run() для async операций
    async def _delete_file():
        async with get_boto_client() as s3:
            await s3.delete_object(...)
    asyncio.run(_delete_file())
```

**Изменения:**
- Event listener теперь синхронный
- Async операции выполняются через `asyncio.run()` или `asyncio.create_task()`
- Добавлена обработка ошибок с логированием

### 3.4 Добавление валидации файлов ✅ ВЫПОЛНЕНО

Создана утилита для валидации:

**Файл**: `app/utils/file_validation.py` (создан)

**Реализовано:**
- ✅ Проверка размера файла (максимум 10 MB)
- ✅ Проверка расширения файла
- ✅ Проверка MIME типа (если доступен)
- ✅ Функция для валидации списка файлов
- ✅ Интеграция в `service_files.py`

**Использование:**
```python
from app.utils.file_validation import validate_files

# В методе create
await validate_files(files)
```

**Чеклист:**
- [x] CORS настроен на конкретные домены (через переменные окружения)
- [x] get_boto_client() используется правильно
- [x] SQLAlchemy events исправлены
- [x] Валидация файлов добавлена

---

## Этап 4: Развертывание базы данных

### 4.1 Обновление docker-compose.yml

Создайте/обновите `docker-compose.yml`:

```yaml
version: "3.9"

services:
  db:
    image: postgres:15
    container_name: teachhelper_postgres
    restart: always
    environment:
      POSTGRES_USER: ${DATABASE_USER}
      POSTGRES_PASSWORD: ${DATABASE_PASSWORD}
      POSTGRES_DB: ${DATABASE_NAME}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./backups:/backups
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DATABASE_USER}"]
      interval: 10s
      timeout: 5s
      retries: 5

  minio:
    image: minio/minio:latest
    container_name: teachhelper_minio
    restart: always
    environment:
      MINIO_ROOT_USER: ${MINIO_USER}
      MINIO_ROOT_PASSWORD: ${MINIO_PASSWORD}
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - minio_data:/data
    command: server /data --console-address ":9001"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 30s
      timeout: 20s
      retries: 3

  app:
    build: .
    container_name: teachhelper_app
    restart: always
    env_file:
      - .env
    ports:
      - "8000:8000"
    depends_on:
      db:
        condition: service_healthy
      minio:
        condition: service_healthy
    volumes:
      - ./public:/app/public
    command: gunicorn main:app -w 4 -k uvicorn.workers.UvicornWorker -b 0.0.0.0:8000

volumes:
  postgres_data:
  minio_data:
```

### 4.2 Запуск базы данных

```bash
# Запуск только БД и MinIO
docker-compose up -d db minio

# Проверка статуса
docker-compose ps

# Проверка логов
docker-compose logs db
```

### 4.3 Создание резервной копии (если есть существующая БД)

```bash
# Экспорт существующей БД
pg_dump -h old_host -U user -d database > backup.sql

# Импорт в новую БД
docker-compose exec db psql -U ${DATABASE_USER} -d ${DATABASE_NAME} < backup.sql
```

**Чеклист:**
- [ ] PostgreSQL контейнер запущен
- [ ] БД доступна и отвечает на запросы
- [ ] Резервная копия создана (если нужно)

---

## Этап 5: Развертывание хранилища файлов

### 5.1 Настройка MinIO

1. **Запуск MinIO** (уже в docker-compose.yml)

2. **Доступ к консоли MinIO**:
   - URL: `http://your-server:9001`
   - Логин: значение из `MINIO_USER`
   - Пароль: значение из `MINIO_PASSWORD`

3. **Создание buckets** (должно создаваться автоматически, но проверьте):
   - `answers` - для ответов студентов
   - `comments` - для файлов в комментариях
   - `tasks` - для файлов заданий

### 5.2 Настройка политик доступа MinIO

В консоли MinIO настройте политики доступа для каждого bucket.

**Чеклист:**
- [ ] MinIO запущен и доступен
- [ ] Все необходимые buckets созданы
- [ ] Политики доступа настроены
- [ ] Тестовая загрузка файла прошла успешно

---

## Этап 6: Развертывание приложения

### 6.1 Сборка Docker образа

```bash
# Сборка образа
docker-compose build app

# Или если используете отдельную команду
docker build -t teachhelper:latest .
```

### 6.2 Применение миграций

```bash
# Вариант 1: Через контейнер
docker-compose run --rm app alembic upgrade head

# Вариант 2: Если приложение уже запущено
docker-compose exec app alembic upgrade head
```

### 6.3 Запуск приложения

```bash
# Запуск всех сервисов
docker-compose up -d

# Проверка статуса
docker-compose ps

# Просмотр логов
docker-compose logs -f app
```

### 6.4 Проверка работоспособности

```bash
# Проверка здоровья API
curl http://localhost:8000/docs

# Проверка эндпоинта
curl http://localhost:8000/auth/me
```

**Чеклист:**
- [ ] Docker образ собран успешно
- [ ] Миграции применены
- [ ] Приложение запущено
- [ ] API отвечает на запросы
- [ ] Логи не содержат критических ошибок

---

## Этап 7: Настройка безопасности

### 7.1 Настройка Nginx (реверс-прокси)

Создайте конфигурацию Nginx:

**Файл**: `/etc/nginx/sites-available/teachhelper`

```nginx
server {
    listen 80;
    server_name yourdomain.com www.yourdomain.com;
    
    # Редирект на HTTPS
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl http2;
    server_name yourdomain.com www.yourdomain.com;

    ssl_certificate /etc/letsencrypt/live/yourdomain.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/yourdomain.com/privkey.pem;
    
    # SSL настройки
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;

    # Логи
    access_log /var/log/nginx/teachhelper_access.log;
    error_log /var/log/nginx/teachhelper_error.log;

    # Проксирование на приложение
    location / {
        proxy_pass http://localhost:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # Таймауты
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    # Ограничение размера загружаемых файлов
    client_max_body_size 20M;
}
```

### 7.2 Установка SSL сертификата (Let's Encrypt)

```bash
# Установка Certbot
sudo apt install certbot python3-certbot-nginx -y

# Получение сертификата
sudo certbot --nginx -d yourdomain.com -d www.yourdomain.com

# Автоматическое обновление
sudo certbot renew --dry-run
```

### 7.3 Настройка файрвола

```bash
# UFW (Ubuntu)
sudo ufw allow 22/tcp    # SSH
sudo ufw allow 80/tcp    # HTTP
sudo ufw allow 443/tcp  # HTTPS
sudo ufw enable

# Проверка
sudo ufw status
```

### 7.4 Добавление Rate Limiting

Установите и настройте rate limiting в Nginx или используйте middleware в FastAPI.

**Чеклист:**
- [ ] Nginx установлен и настроен
- [ ] SSL сертификат установлен
- [ ] HTTPS работает
- [ ] Файрвол настроен
- [ ] Rate limiting настроен

---

## Этап 8: Мониторинг и логирование

### 8.1 Настройка логирования

**Файл**: `app/utils/logger.py` (убедитесь, что настроен)

```python
import logging
import sys

logging.basicConfig(
    level=logging.INFO,
    format='%(asctime)s - %(name)s - %(levelname)s - %(message)s',
    handlers=[
        logging.FileHandler('app.log'),
        logging.StreamHandler(sys.stdout)
    ]
)
```

### 8.2 Настройка ротации логов

**Файл**: `/etc/logrotate.d/teachhelper`

```
/path/to/teachHelper/app.log {
    daily
    rotate 7
    compress
    delaycompress
    missingok
    notifempty
    create 0640 app app
}
```

### 8.3 Мониторинг (опционально)

Рассмотрите использование:
- **Prometheus + Grafana** - для метрик
- **Sentry** - для отслеживания ошибок
- **Uptime monitoring** - для проверки доступности

**Чеклист:**
- [ ] Логирование настроено
- [ ] Ротация логов настроена
- [ ] Мониторинг настроен (если используется)

---

## Этап 9: Тестирование в продакшене

### 9.1 Функциональное тестирование

1. **Регистрация пользователя**:
   - [ ] Регистрация работает
   - [ ] Email подтверждения отправляется
   - [ ] Подтверждение email работает

2. **Аутентификация**:
   - [ ] Вход работает
   - [ ] JWT токен выдается
   - [ ] Защищенные эндпоинты требуют токен

3. **Создание заданий**:
   - [ ] Учитель может создать задание
   - [ ] Упражнения и критерии создаются

4. **Загрузка файлов**:
   - [ ] Файлы загружаются
   - [ ] Валидация работает
   - [ ] Файлы сохраняются в MinIO

5. **Проверка работ**:
   - [ ] Учитель может проверить работу
   - [ ] Комментарии создаются
   - [ ] Оценки выставляются

### 9.2 Нагрузочное тестирование

```bash
# Используйте Apache Bench или wrk
ab -n 1000 -c 10 https://yourdomain.com/api/endpoint
```

### 9.3 Тестирование безопасности

- [ ] SQL injection защита работает
- [ ] XSS защита работает
- [ ] CORS настроен правильно
- [ ] Файлы валидируются
- [ ] Rate limiting работает

**Чеклист:**
- [ ] Все основные функции работают
- [ ] Производительность приемлемая
- [ ] Безопасность проверена

---

## Этап 10: Запуск и поддержка

### 10.1 Финальная проверка

```bash
# Проверка всех сервисов
docker-compose ps

# Проверка логов
docker-compose logs --tail=100

# Проверка дискового пространства
df -h

# Проверка использования ресурсов
docker stats
```

### 10.2 Создание первого администратора

```bash
# Через API или напрямую в БД
docker-compose exec db psql -U ${DATABASE_USER} -d ${DATABASE_NAME}

# В psql:
UPDATE users SET role = 'admin', is_verificated = true WHERE email = 'admin@example.com';
```

### 10.3 Настройка резервного копирования

**Скрипт резервного копирования БД**:

```bash
#!/bin/bash
# backup.sh

DATE=$(date +%Y%m%d_%H%M%S)
BACKUP_DIR="/backups"
DB_NAME="teachhelper_prod"

docker-compose exec -T db pg_dump -U ${DATABASE_USER} ${DB_NAME} > ${BACKUP_DIR}/db_${DATE}.sql

# Удаление старых бэкапов (старше 30 дней)
find ${BACKUP_DIR} -name "db_*.sql" -mtime +30 -delete
```

**Добавьте в crontab**:
```bash
# Ежедневное резервное копирование в 2:00
0 2 * * * /path/to/backup.sh
```

### 10.4 Документация для пользователей

Создайте руководство пользователя:
- Как зарегистрироваться
- Как создать задание
- Как проверить работу
- FAQ

### 10.5 План поддержки

- [ ] Настроены уведомления об ошибках
- [ ] Настроен мониторинг доступности
- [ ] Создан план резервного копирования
- [ ] Документация для пользователей готова
- [ ] Контакты поддержки указаны

---

## 📊 Чеклист финальной проверки

### Инфраструктура
- [ ] Сервер настроен и защищен
- [ ] Docker и Docker Compose установлены
- [ ] Все сервисы запущены и работают

### Безопасность
- [ ] CORS настроен правильно
- [ ] SSL сертификат установлен
- [ ] Файрвол настроен
- [ ] Секретные ключи сгенерированы
- [ ] Валидация файлов работает
- [ ] Rate limiting настроен

### Приложение
- [ ] Миграции применены
- [ ] Приложение запущено
- [ ] API доступен
- [ ] Логирование работает

### Тестирование
- [ ] Функциональное тестирование пройдено
- [ ] Безопасность проверена
- [ ] Производительность приемлемая

### Поддержка
- [ ] Резервное копирование настроено
- [ ] Мониторинг настроен
- [ ] Документация готова

---

## 🚨 Процедуры в случае проблем

### Откат к предыдущей версии

```bash
# Остановка текущей версии
docker-compose down

# Откат миграций (если нужно)
docker-compose run --rm app alembic downgrade -1

# Запуск предыдущей версии
docker-compose up -d
```

### Восстановление из резервной копии

```bash
# Остановка приложения
docker-compose stop app

# Восстановление БД
docker-compose exec -T db psql -U ${DATABASE_USER} ${DATABASE_NAME} < backup.sql

# Запуск приложения
docker-compose start app
```

### Просмотр логов для диагностики

```bash
# Логи приложения
docker-compose logs app --tail=100 -f

# Логи БД
docker-compose logs db --tail=100 -f

# Логи Nginx
sudo tail -f /var/log/nginx/teachhelper_error.log
```

---

## 📞 Контакты поддержки

- **Техническая поддержка**: [email/телефон]
- **Документация**: [ссылка]
- **Репозиторий**: [ссылка]

---

**Дата создания документа**: [дата]  
**Версия**: 1.0  
**Последнее обновление**: [дата]

