# TeachHelper - Система проверки письменных домашних работ

## 📋 Описание проекта

**TeachHelper** — веб-приложение для учителей русского языка, позволяющее проверять письменные домашние работы студентов. Система поддерживает создание заданий с критериями оценки, загрузку работ студентов, комментирование и выставление оценок.

### Основные возможности

- ✅ **Управление пользователями**: Регистрация и аутентификация с подтверждением email
- ✅ **Создание заданий**: Учителя могут создавать задания с упражнениями и критериями оценки
- ✅ **Проверка работ**: Система статусов работ (черновик, в процессе, на проверке, проверено)
- ✅ **Комментирование**: Учителя могут оставлять комментарии к ответам студентов
- ✅ **Загрузка файлов**: Поддержка загрузки файлов для заданий и комментариев
- ✅ **Классы**: Организация студентов в классы для удобного управления
- ✅ **Оценка по критериям**: Детальная оценка по каждому критерию упражнения

## 🏗️ Архитектура проекта

### Технологический стек

- **Backend Framework**: FastAPI 0.119.0
- **Database**: PostgreSQL 15 (async через asyncpg)
- **ORM**: SQLAlchemy 2.0.44 (async)
- **File Storage**: MinIO (S3-совместимое хранилище)
- **Authentication**: JWT (python-jose)
- **Password Hashing**: Argon2/bcrypt
- **Migrations**: Alembic
- **Testing**: pytest, pytest-asyncio
- **Email**: aiosmtplib, fastapi-mail

### Структура проекта

```
teachHelper/
├── app/
│   ├── config/          # Конфигурация (БД, boto, mail)
│   ├── db.py            # Настройка подключения к БД
│   ├── exceptions/       # Кастомные исключения
│   ├── models/          # SQLAlchemy модели
│   ├── repositories/    # Слой доступа к данным
│   ├── routes/          # API эндпоинты
│   ├── schemas/         # Pydantic схемы для валидации
│   ├── services/        # Бизнес-логика
│   ├── utils/           # Утилиты (auth, password, logger)
│   └── templates/       # Email шаблоны
├── alembic/             # Миграции БД
├── tests/               # Тесты
│   ├── integration_tests/
│   └── unit_tests/
├── public/              # Статические файлы
├── main.py              # Точка входа приложения
├── docker-compose.yml   # Docker конфигурация
├── Dockerfile           # Образ приложения
├── requirements.txt     # Зависимости Python
└── pytest.ini          # Конфигурация тестов
```

### Архитектурные слои

1. **Routes (API Layer)**: FastAPI роутеры, обработка HTTP запросов
2. **Services (Business Logic)**: Бизнес-логика приложения
3. **Repositories (Data Access)**: Абстракция доступа к данным
4. **Models (Database)**: SQLAlchemy ORM модели

### Модели данных

#### Основные сущности:

- **Users**: Пользователи (admin, teacher, student)
- **Subjects**: Предметы (например, "Русский язык")
- **Tasks**: Задания, созданные учителем
- **Exercises**: Упражнения в рамках задания
- **Criterions**: Критерии оценки для упражнений
- **Works**: Работы студентов по заданиям
- **Answers**: Ответы студентов на упражнения
- **Assessments**: Оценка по критериям (заполняется учителем)
- **Comments**: Комментарии учителя к ответам
- **Files**: Загруженные файлы
- **Classrooms**: Классы студентов

## 🚀 Быстрый старт

### Предварительные требования

- Python 3.12+
- PostgreSQL 15+
- MinIO (или S3-совместимое хранилище)
- Docker и Docker Compose (опционально)

### Установка для разработки

1. **Клонируйте репозиторий** (если есть):
```bash
git clone <repository-url>
cd teachHelper
```

2. **Создайте виртуальное окружение**:
```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows
```

3. **Установите зависимости**:
```bash
pip install -r requirements.txt
```

4. **Настройте переменные окружения**:
Создайте файл `.env` в корне проекта:
```env
# Database
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_USER=postgres
DATABASE_PASSWORD=your_password
DATABASE_NAME=teachhelper

# Security
SECRET=your-secret-key-here-min-32-chars
SECRET_CONFIRM_KEY=your-confirm-key-here
SECRET_RESET_KEY=your-reset-key-here
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=120

# Frontend
FRONT_URL=http://localhost:3000

# MinIO
MINIO_HOST=localhost
MINIO_PORT=9000
MINIO_USER=minioadmin
MINIO_PASSWORD=minioadmin

# Email (для подтверждения и восстановления пароля)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_TLS=true
```

5. **Запустите PostgreSQL и MinIO**:
```bash
docker-compose up -d db
# MinIO нужно запустить отдельно или добавить в docker-compose.yml
```

6. **Примените миграции**:
```bash
alembic upgrade head
```

7. **Запустите приложение**:
```bash
python main.py
# или
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

8. **Откройте документацию API**:
```
http://localhost:8000/docs
```

### Запуск через Docker

1. **Настройте `.env` файл** (см. выше)

2. **Соберите и запустите контейнеры**:
```bash
docker-compose up -d --build
```

3. **Примените миграции** (если не применены автоматически):
```bash
docker-compose exec app alembic upgrade head
```

## 📚 API Документация

### Базовый URL
```
http://localhost:8000
```

### Автоматическая документация

FastAPI автоматически генерирует интерактивную документацию:
- **Swagger UI**: `http://localhost:8000/docs`
- **ReDoc**: `http://localhost:8000/redoc`

### Основные эндпоинты

#### Аутентификация (`/auth`)
- `POST /auth/register` - Регистрация нового пользователя
- `POST /auth/login` - Вход в систему
- `POST /auth/send_email_confirmation_link` - Отправка ссылки подтверждения email
- `POST /auth/confirm_email` - Подтверждение email
- `POST /auth/forgot_password` - Запрос на восстановление пароля
- `POST /auth/reset_password` - Сброс пароля
- `GET /auth/me` - Получение информации о текущем пользователе

#### Задания (`/tasks`)
- `POST /tasks` - Создание задания (только teacher)
- `GET /tasks` - Список заданий с фильтрами
- `GET /tasks/{id}` - Получение задания по ID
- `PUT /tasks/{id}` - Обновление задания
- `DELETE /tasks/{id}` - Удаление задания
- `POST /tasks/{task_id}/start` - Создание работ для студентов

#### Работы (`/works`)
- `GET /works/teacher` - Список работ для учителя
- `GET /works/student` - Список работ для студента
- `GET /works/{id}` - Получение работы по ID
- `PUT /works/{id}` - Обновление работы (оценка)
- `POST /works/{id}/comments` - Создание комментария
- `PUT /works/{id}/comments/{comment_id}` - Обновление комментария
- `DELETE /works/{id}/comments/{comment_id}` - Удаление комментария

#### Классы (`/classrooms`)
- `POST /classrooms` - Создание класса
- `GET /classrooms` - Список классов
- `GET /classrooms/{id}` - Получение класса
- `PUT /classrooms/{id}` - Обновление класса
- `DELETE /classrooms/{id}` - Удаление класса

#### Предметы (`/subjects`)
- `POST /subjects` - Создание предмета (только admin)
- `GET /subjects` - Список предметов
- `PUT /subjects/{id}` - Обновление предмета
- `DELETE /subjects/{id}` - Удаление предмета

### Аутентификация

Все защищенные эндпоинты требуют JWT токен в заголовке:
```
Authorization: Bearer <token>
```

Токен получается при успешном входе через `/auth/login`.

## 🧪 Тестирование

### Запуск тестов

```bash
# Все тесты
pytest

# С покрытием
pytest --cov=app --cov-report=html

# Только интеграционные
pytest tests/integration_tests/

# Только unit тесты
pytest tests/unit_tests/
```

### Настройка тестов

Тесты используют отдельную тестовую БД. Настройте `.env_test`:
```env
DATABASE_NAME=teachhelper_test
# ... остальные настройки как в .env
```

## 🔧 Конфигурация

### Переменные окружения

| Переменная | Описание | Обязательно |
|-----------|----------|-------------|
| `DATABASE_HOST` | Хост PostgreSQL | Да |
| `DATABASE_PORT` | Порт PostgreSQL | Да |
| `DATABASE_USER` | Пользователь БД | Да |
| `DATABASE_PASSWORD` | Пароль БД | Да |
| `DATABASE_NAME` | Имя БД | Да |
| `SECRET` | Секретный ключ для JWT | Да |
| `SECRET_CONFIRM_KEY` | Ключ для подтверждения email | Да |
| `SECRET_RESET_KEY` | Ключ для сброса пароля | Да |
| `FRONT_URL` | URL фронтенд приложения | Да |
| `MINIO_HOST` | Хост MinIO | Да |
| `MINIO_PORT` | Порт MinIO | Да |
| `MINIO_USER` | Пользователь MinIO | Да |
| `MINIO_PASSWORD` | Пароль MinIO | Да |

## 📝 Миграции БД

### Создание новой миграции

```bash
alembic revision --autogenerate -m "описание изменений"
```

### Применение миграций

```bash
alembic upgrade head
```

### Откат миграции

```bash
alembic downgrade -1
```

## 🐛 Известные проблемы и ограничения

1. **CORS настроен на `*`** - необходимо ограничить для продакшена
2. **Нет валидации размера и типа файлов** - требуется добавить
3. **Нет rate limiting** - рекомендуется добавить для защиты от DDoS
4. **Нет пагинации** - для больших списков может быть медленно

## 🔒 Безопасность

### Рекомендации для продакшена

1. ✅ Используйте HTTPS
2. ✅ Ограничьте CORS на конкретные домены
3. ✅ Добавьте rate limiting
4. ✅ Валидируйте все загружаемые файлы
5. ✅ Используйте сильные секретные ключи (минимум 32 символа)
6. ✅ Регулярно обновляйте зависимости
7. ✅ Настройте мониторинг и логирование
8. ✅ Используйте переменные окружения для секретов (не храните в коде)

## 📖 Дополнительная документация

- [План внедрения (DEPLOYMENT.md)](DEPLOYMENT.md)
- [FastAPI документация](https://fastapi.tiangolo.com/)
- [SQLAlchemy документация](https://docs.sqlalchemy.org/)
- [Alembic документация](https://alembic.sqlalchemy.org/)

## 👥 Роли пользователей

- **Admin**: Полный доступ, управление предметами
- **Teacher**: Создание заданий, проверка работ, управление классами
- **Student**: Просмотр заданий, загрузка работ, просмотр оценок

## 📄 Лицензия

[Укажите лицензию проекта]

## 🤝 Вклад в проект

[Описание процесса контрибуции, если применимо]

## 📧 Контакты

[Контактная информация]

